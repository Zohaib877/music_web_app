/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['via.placeholder.com','testcodex.com', 'images.remotePatterns'], 
    },
  };
export default nextConfig;
